// src/utils/milestones.js
// "Next milestone" dashboard widget — always shows the player something just out of reach (Civ's
// "just one more turn" mechanism). Empty for now: every milestone it used to compute progress
// against (tech points, capitals-held, hostile-bloc pacification) was tied to the old resource
// model and the Israel campaign's specific nations. Re-authoring this against the new resource
// model and victory conditions is later work (Phase D), once there's real per-turn progress to
// show toward each one.

export const computeMilestones = () => [];
