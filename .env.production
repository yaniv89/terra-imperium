# Cloud saves (plan §M0.5). VITE_SUPABASE_ANON_KEY is the Supabase "anon"/"publishable" key —
# it is meant to be public (it ships in every client bundle) and safe to commit; Row Level
# Security policies in supabase/migrations/*.sql are what actually protect the data. Never put
# the "service_role"/secret key here or anywhere else in this repo.
VITE_SUPABASE_URL=https://visxnnbemirhpigvwiry.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_aobvW_XKHP6bO_-KLq12mA_XdSpu80U
